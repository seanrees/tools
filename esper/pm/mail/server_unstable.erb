-- AUTOMATED MESSAGE --

UNSTABLE SERVER: <%= name %>

This server has failed <%= fail_count %> times over the last <%= first_failed %>.

The last successful connection occurred <%= last_successful_connect %>.

No action has been taken.

When this server stabilizes, an update email will be sent.

-- AUTOMATED MESSAGE --
